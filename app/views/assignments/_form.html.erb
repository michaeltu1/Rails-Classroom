<%= form_with(model: assignment, local: true) do |form| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lecture_title %>
    <%= form.text_area :lecture_title, id: :assignment_lecture_title %>
  </div>

  <div class="field">
    <%= form.label :reading %>
    <%= form.text_area :reading, id: :assignment_reading %>
  </div>

  <div class="field">
    <%= form.label :homework %>
    <%= form.text_area :homework, id: :assignment_homework %>
  </div>

  <div class="field">
    <%= form.label :attachment %>
    <%= form.text_field :attachment, id: :assignment_attachment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
